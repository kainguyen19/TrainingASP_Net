@model Lab3.ViewModels.CategoryViewModel

@{
    ViewBag.Title = "Edit";
}

<h2>Update Category information</h2>
<hr />

@using (Html.BeginForm("Edit", "Category", FormMethod.Post))
{
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true, "", new { @class = "text-danger" })

    @Html.HiddenFor(model => model.ID)

    <div class="form-group row">
        @Html.LabelFor(model => model.Name, new { @class = "control-label col-md-1" })
        <div class="col-md-8">
            @Html.TextBoxFor(model => model.Name, new { @class = "form-control" })
            @Html.ValidationMessageFor(model => model.Name, "", new { @class = "text-danger" })
        </div>
    </div>
    <div class="form-group row">
        <div class="col-md-10">
            <button type="submit" class="btn btn-primary">Save new category information</button>
            @Html.ActionLink("Back to list of Category", "Index", "Category", new { @class = "btn btn-success" })
        </div>
    </div>
}
